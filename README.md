# Phishing-Website-Detection-System

Short description: A project that detects phishing websites using feature extraction + ML/classical models.

## Contents
- `src/` — source code
- `notebooks/` — exploratory notebooks (if any)
- `data/` — dataset (do not commit large raw datasets)
- `models/` — saved model files (keep large files out of repo)

## Quick setup
1. Create & activate virtual environment:
```bash
python -m venv venv
# Windows PowerShell
venv\Scripts\Activate.ps1
# or cmd
venv\Scripts\activate.bat
